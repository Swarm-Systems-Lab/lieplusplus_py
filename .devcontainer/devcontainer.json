{
  "name": "lieplusplus-py",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-vscode.cmake-tools",
        "ms-vscode.cpptools",
        "charliermarsh.ruff",
        "ms-python.mypy-type-checker"
      ]
    }
  },
  "postCreateCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && curl --proto \"=https\" --tlsv1.2 -sSf https://just.systems/install.sh | bash",
  "postStartCommand": "uv sync --all-groups",
  "remoteUser": "vscode"
}
